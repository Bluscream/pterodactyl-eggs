{
    "_comment": "DayZ Mod on Arma 2: Operation Arrowhead, Linux. DRAFT -- UNTESTED, see README.md. Layout derived from denisio/Dayz-Linux-Server (the only maintained Linux DayZ Mod layout) and Bluscream/AMPTemplates arma2oa.kvp. Must use an image with i386 multiarch: the A2OA engine is 32-bit. NOTE: games:dayz is deliberately NOT offered -- its entrypoint derives CLIENT_MODS from MODIFICATIONS, auto-updates via STEAMCMD_APPID and downloads workshop mods for DayZ appid 221100, all wrong here. games:source's entrypoint only auto-updates when SRCDS_APPID is set, which this egg does not set.",
    "meta": {
        "version": "PTDL_v2",
        "update_url": null
    },
    "exported_at": "2026-09-13T18:12:26+00:00",
    "name": "DayZ Mod (Arma 2 OA)",
    "author": "bluscreamlp@gmail.com",
    "description": "The original DayZ Mod running on an Arma 2: Operation Arrowhead Linux dedicated server. Persistence uses the @hive + writer.pl Perl path, because the official Hive extension is a Windows DLL with no Linux build. Requires an EXTERNAL MySQL database -- see README.",
    "features": [
        "steam_disk_space"
    ],
    "docker_images": {
        "Debian (i386 libs)": "ghcr.io/parkervcp/games:source"
    },
    "file_denylist": [],
    "startup": "bash ./boot.sh -server -config={{PROFILE_DIR}}/server.cfg -cfg={{PROFILE_DIR}}/basic.cfg -port={{SERVER_PORT}} -ip=0.0.0.0 -profiles={{PROFILE_DIR}} -name={{PROFILE_DIR}} -bepath=./battleye -beta=\"{{BETA_PATH}}\" -world={{WORLD}} -mod=\"{{MODIFICATIONS}}\" -serverMod=\"{{SERVERMODS}}\" -pid=server.pid {{STARTUP_PARAMS}}",
    "config": {
        "files": "{\n    \"cfgdayz/server.cfg\": {\n        \"parser\": \"file\",\n        \"find\": {\n            \"hostname =\": \"hostname = \\\"{{env.SERVER_NAME}}\\\";\",\n            \"password =\": \"password = \\\"{{env.SERVER_PASSWORD}}\\\";\",\n            \"passwordAdmin =\": \"passwordAdmin = \\\"{{env.ADMIN_PASSWORD}}\\\";\",\n            \"maxPlayers =\": \"maxPlayers = {{env.MAX_PLAYERS}};\",\n            \"verifySignatures =\": \"verifySignatures = {{env.VERIFY_SIGNATURES}};\",\n            \"requiredBuild =\": \"requiredBuild = {{env.REQUIRED_BUILD}};\"\n        }\n    }\n}",
        "startup": "{\"done\":\"Dedicated host created.\"}",
        "logs": "{}",
        "stop": "^C"
    },
    "scripts": {
        "installation": {
            "script": "#!/bin/bash\n# Pterodactyl install script -- DayZ Mod (Arma 2: Operation Arrowhead) dedicated server, Linux\n#\n# DayZ Mod has no dedicated-server appid. A working server is assembled from five pieces:\n#\n#   1. Arma 2 OA game data       -- appid 33930 (Windows data; the engine binary is separate)\n#   2. Arma 2 base content       -- appid 33900 (DayZ Mod loads A2 buildings and vehicles)\n#   3. The Linux server engine   -- 1.59.84216 BETA standalone package (mirror)\n#   4. @dayz                     -- the mod, from Steam appid 224580 or a URL\n#   5. @hive + writer.pl         -- the Linux persistence path, from denisio/Dayz-Linux-Server\n#\n# Piece 5 is the awkward one. The official Hive is a Windows DLL with no Linux build, so the Linux\n# route is @hive plus a Perl writer fed by the engine's stdout. That writer needs JSON::XS, DBI and\n# DBD::mysql, none of which are in Debian's perl-base -- and apt-installing them here would be\n# pointless, because this runs in a throwaway installer container. They are therefore VENDORED onto\n# the persistent volume and reached via PERL5LIB at runtime.\n#\n# MySQL is not installed and cannot be: Pterodactyl runs one process per container. Point HIVE_DB_*\n# at an external server and load database.sql into it once by hand.\n\nset -e\n\n# Install-time dependencies. gcc is not optional: the A2OA package's own `install` compiles\n# tolower.c with it, and without the lowercase pass the engine cannot find its addons.\napt -y update\napt -y --no-install-recommends install curl ca-certificates bzip2 unzip gcc file \\\n    libjson-xs-perl libdbi-perl libdbd-mysql-perl\n\nif [[ \"${STEAM_USER}\" == \"\" ]] || [[ \"${STEAM_USER}\" == \"anonymous\" ]] || [[ \"${STEAM_PASS}\" == \"\" ]]; then\n    echo \"INSTALLATION ERROR: a real Steam account owning Arma 2 AND Operation Arrowhead is\"\n    echo \"  required. Neither 33900 nor 33930 is available to the anonymous account.\"\n    exit 1\nfi\n\ncd /tmp\nmkdir -p /mnt/server/steamcmd /mnt/server/steamapps\ncurl --fail -sSL -o steamcmd.tar.gz https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz\ntar -xzf steamcmd.tar.gz -C /mnt/server/steamcmd\ncd /mnt/server/steamcmd\n\nchown -R root:root /mnt\n# HOME on the persistent volume: the Steam sentry from a successful Guard login\n# (Steam/config/ssfn*) then outlives this container, so later reinstalls may need no code.\nexport HOME=/mnt/server\n\n# --- Steam Guard / 2FA ---------------------------------------------------------------------\n# STEAM_AUTH holds EITHER a literal 5-character code OR a URL returning one; the mode is\n# inferred from the value. A URL matters more here than in any other egg in this repo: there are\n# THREE logins below, and a TOTP is single-use with a ~30 second life.\n#\n# Prefer the .steam_auth file for a URL that embeds an ASF IPC password -- panel variables are\n# stored in the database in plain text and readable by any panel admin.\nGUARD_FILE=\"/mnt/server/.steam_auth\"\n[[ -z \"${STEAM_AUTH}\" && -f \"${GUARD_FILE}\" ]] && STEAM_AUTH=\"$(tr -d ' \\r\\n' < \"${GUARD_FILE}\")\"\n\nis_url() { [[ \"${1}\" =~ ^https?:// ]]; }\n\nmint_2fa_code() {\n    local src=\"${STEAM_AUTH}\"\n    [[ -z \"${src}\" ]] && return 0\n    if ! is_url \"${src}\"; then echo \"${src}\"; return 0; fi\n    local ipc_pw=\"\"\n    case \"${src}\" in *password=*) ipc_pw=\"${src##*password=}\"; ipc_pw=\"${ipc_pw%%&*}\" ;; esac\n    local response\n    response=\"$(curl --fail -sSL --max-time 15 -H \"Authentication: ${ipc_pw}\" \"${src}\" 2>/dev/null)\"\n    [[ -z \"${response}\" ]] && response=\"$(curl --fail -sSL --max-time 15 \"${src}\" 2>/dev/null)\"\n    if [[ -z \"${response}\" ]]; then\n        echo \"  2FA endpoint returned nothing (auth rejected or unreachable).\" >&2\n        return 0\n    fi\n    # ASF's shape first (last match -- the wrapper reuses the key), then a bare body, then any\n    # uppercase 5-char token. The first step stops a 5-character uppercase BOT NAME winning.\n    local code bare\n    code=\"$(echo \"${response}\" | grep -oE '\"Result\"[[:space:]]*:[[:space:]]*\"[A-Za-z0-9]{5}\"' \\\n            | tail -1 | grep -oE '[A-Za-z0-9]{5}\"$' | tr -d '\"')\"\n    if [[ -z \"${code}\" ]]; then\n        bare=\"$(echo \"${response}\" | tr -d ' \\r\\n')\"\n        [[ \"${bare}\" =~ ^[A-Za-z0-9]{5}$ ]] && code=\"${bare}\"\n    fi\n    [[ -z \"${code}\" ]] && code=\"$(echo \"${response}\" | grep -oE '[A-Z0-9]{5}' | head -1)\"\n    echo \"${code}\" | tr '[:lower:]' '[:upper:]'\n}\n\nsteam_app_update() { # appid, install_dir\n    local appid=\"${1}\" dir=\"${2}\" attempt code log=\"/tmp/steamcmd-${1}.log\"\n    for attempt in 1 2; do\n        code=\"$(mint_2fa_code)\"\n        echo \"Downloading appid ${appid} (attempt ${attempt})...\"\n        # Unquoted ${code}: steamcmd takes the Guard code as +login's third positional argument, and\n        # an empty value must vanish rather than become an empty argument. stdin is /dev/null and the\n        # call is time-boxed, or a Guard prompt would block forever on a tty that does not exist.\n        timeout 3600 ./steamcmd.sh +force_install_dir \"${dir}\" \\\n            +login \"${STEAM_USER}\" \"${STEAM_PASS}\" ${code} \\\n            +app_update \"${appid}\" ${STEAMCMD_EXTRA_FLAGS} validate +quit < /dev/null 2>&1 | tee \"${log}\"\n        # Judged by the app manifest, not steamcmd's exit status, which is non-zero on good runs.\n        # (A pipeline's status is tee's anyway, so testing it directly always reports success.)\n        if [[ -f \"${dir}/steamapps/appmanifest_${appid}.acf\" ]] \\\n           && ! grep -qiE 'Login Failure|two-factor|Invalid Password|Rate Limit' \"${log}\"; then\n            return 0\n        fi\n        if [[ \"${attempt}\" == \"1\" ]]; then\n            echo \"  Login or download failed.\"\n            if is_url \"${STEAM_AUTH}\"; then\n                # A TOTP only rolls over every 30s; minting again now returns the same rejected code.\n                echo \"  Waiting 31s for a new TOTP window, then retrying once...\"\n                sleep 31\n            fi\n        fi\n    done\n    echo \"FATAL: could not download appid ${appid}. Check the credentials, that the account owns\"\n    echo \"  Arma 2 and Operation Arrowhead, and that STEAM_AUTH is valid or reachable.\"\n    return 1\n}\n\n# --- 1 + 2. Game data -----------------------------------------------------------------------\nsteam_app_update 33930 /mnt/server\nsteam_app_update 33900 /mnt/server\n\nmkdir -p /mnt/server/.steam/sdk32\ncp -v linux32/steamclient.so /mnt/server/.steam/sdk32/steamclient.so || true\n\n# --- 3. Linux server engine -----------------------------------------------------------------\ncd /mnt/server\ncurl --fail -sSL -o /tmp/a2oa-server.tar.bz2 \"${A2OA_SERVER_URL}\" || {\n    echo \"FATAL: could not download the A2OA Linux server package from ${A2OA_SERVER_URL}\"\n    exit 1\n}\ntar -xjf /tmp/a2oa-server.tar.bz2 -C /mnt/server\nrm -f /tmp/a2oa-server.tar.bz2\n\n# --- 4. The mod ------------------------------------------------------------------------------\nif [[ -n \"${DAYZ_MOD_URL}\" ]]; then\n    echo \"Fetching @dayz from ${DAYZ_MOD_URL}...\"\n    mkdir -p /tmp/dayzmod\n    curl --fail -sSL -o /tmp/dayzmod.archive \"${DAYZ_MOD_URL}\" || {\n        echo \"FATAL: could not download ${DAYZ_MOD_URL}\"\n        exit 1\n    }\n    cd /tmp/dayzmod\n    if file /tmp/dayzmod.archive | grep -qi zip; then unzip -oq /tmp/dayzmod.archive\n    elif file /tmp/dayzmod.archive | grep -qi bzip2; then tar -xjf /tmp/dayzmod.archive\n    else tar -xzf /tmp/dayzmod.archive 2>/dev/null || unzip -oq /tmp/dayzmod.archive; fi\n    cp -r /tmp/dayzmod/* /mnt/server/ 2>/dev/null || true\n    rm -rf /tmp/dayzmod /tmp/dayzmod.archive\nelif [[ \"${DAYZ_MOD_FROM_STEAM}\" == \"1\" ]]; then\n    cd /mnt/server/steamcmd\n    # 224580 is free to own but still needs the real account. Downloaded to a side directory so a\n    # client-shaped layout cannot scatter files across the server root.\n    steam_app_update 224580 /mnt/server/_dayzmod || \\\n        echo \"WARNING: appid 224580 download failed -- supply DAYZ_MOD_URL instead.\"\n    find /mnt/server/_dayzmod -maxdepth 3 -iname '@dayz*' -type d -exec cp -r {} /mnt/server/ \\; 2>/dev/null || true\n    rm -rf /mnt/server/_dayzmod\nelse\n    echo \"NOTE: no @dayz source configured (DAYZ_MOD_URL empty, DAYZ_MOD_FROM_STEAM=0).\"\n    echo \"  Upload @dayz into the server root before starting.\"\nfi\n\n# --- 5. @hive, writer.pl and the Linux layout ------------------------------------------------\ncd /tmp\ncurl --fail -sSL -o linuxsrv.tar.gz \"${HIVE_REPO_URL}\" || {\n    echo \"FATAL: could not download the Hive layout from ${HIVE_REPO_URL}\"\n    exit 1\n}\nmkdir -p /tmp/linuxsrv && tar -xzf linuxsrv.tar.gz -C /tmp/linuxsrv --strip-components=1\nfor item in @hive writer.pl restarter.pl tolower.c install database.sql object_init_data.txt \\\n            cfgdayz mpmissions expansion cache; do\n    [[ -e \"/tmp/linuxsrv/${item}\" ]] && cp -rn \"/tmp/linuxsrv/${item}\" /mnt/server/ 2>/dev/null || true\ndone\nrm -rf /tmp/linuxsrv /tmp/linuxsrv.tar.gz\n\n# Vendor the Perl modules writer.pl needs onto the VOLUME. apt cannot help at runtime: this\n# container is discarded, and the game images ship only perl-base. Copying the vendor trees works\n# because installer and runtime images are both Debian bookworm; boot.sh verifies the modules\n# actually load and disables the Hive with a clear message if they do not.\necho \"Vendoring Perl modules (JSON::XS, DBI, DBD::mysql) into ./perl5...\"\nmkdir -p /mnt/server/perl5\nfor d in /usr/lib/*/perl5/* /usr/share/perl5; do\n    [[ -d \"${d}\" ]] && cp -rn \"${d}/.\" /mnt/server/perl5/ 2>/dev/null || true\ndone\nif [[ ! -f /mnt/server/perl5/JSON/XS.pm ]] || [[ ! -f /mnt/server/perl5/DBI.pm ]]; then\n    echo \"WARNING: the Perl modules did not land in ./perl5 as expected. Persistence will be\"\n    echo \"  disabled at boot. Check the paths above against this installer image's layout.\"\nfi\n\n# --- Lowercase conversion --------------------------------------------------------------------\n# Mandatory, and only meaningful once every piece is in place.\ncd /mnt/server\nif [[ -f install ]]; then\n    chmod +x install && ./install\nelse\n    echo \"FATAL: the A2OA package contained no 'install' script, so filenames were not\"\n    echo \"  lowercased. The engine cannot load its addons in that state.\"\n    exit 1\nfi\nrm -f ./*.exe ./*.chm ./*.dll 2>/dev/null || true\nfind ./battleye ./expansion -maxdepth 2 -name '*.dll' -delete 2>/dev/null || true\n\nif [[ ! -f /mnt/server/server ]]; then\n    echo \"FATAL: ./server not found after extracting the A2OA package.\"\n    exit 1\nfi\nchmod +x /mnt/server/server\n\n# --- Egg companion files ---------------------------------------------------------------------\n# Pinned to a ref so reinstalls are reproducible. Bump RAW_REF when these change.\nRAW_REF=\"master\"\nRAW_BASE=\"https://raw.githubusercontent.com/bluscream/pterodactyl-eggs/${RAW_REF}/dayz-mod\"\n\nfetch() { # source filename, destination path, overwrite(0|1)\n    if [[ \"${3}\" == \"0\" && -f \"/mnt/server/${2}\" ]]; then\n        echo \"Keeping existing ${2}.\"\n        return 0\n    fi\n    echo \"Fetching ${1}...\"\n    # --fail is mandatory: without it curl exits 0 on a 404 and writes the error page to the file.\n    curl --fail -sSL -o \"/mnt/server/${2}.tmp\" \"${RAW_BASE}/${1}\" || {\n        echo \"FATAL: could not download ${1} from ${RAW_BASE}\"\n        rm -f \"/mnt/server/${2}.tmp\"\n        exit 1\n    }\n    mv \"/mnt/server/${2}.tmp\" \"/mnt/server/${2}\"\n}\n\nmkdir -p /mnt/server/cfgdayz /mnt/server/battleye /mnt/server/keys /mnt/server/cache\nfetch boot.sh boot.sh 1                  # egg logic -- always refreshed\nfetch server.cfg cfgdayz/server.cfg 0    # user config -- never clobbered\nchmod +x /mnt/server/boot.sh\nchmod 644 /mnt/server/cfgdayz/server.cfg\n\n# Consume a stale single-use code; keep a URL, which stays valid.\nif [[ -f \"${GUARD_FILE}\" ]] && ! is_url \"$(tr -d ' \\r\\n' < \"${GUARD_FILE}\")\"; then\n    rm -f \"${GUARD_FILE}\"\n    echo \"Consumed and removed .steam_auth (single-use).\"\nfi\n\necho \"-----------------------------------------\"\necho \"Installation completed.\"\necho\necho \"REMAINING MANUAL STEP -- the server runs but will NOT persist until this is done:\"\necho \"  1. Create a MySQL database and user on a host this container can reach.\"\necho \"  2. Load /home/container/database.sql into it.\"\necho \"  3. Import object_init_data.txt into Object_DATA and Object_init_DATA.\"\necho \"  4. Set HIVE_DB_HOST / _PORT / _NAME / _USER / _PASS in the panel.\"\necho \"     boot.sh patches writer.pl to read those -- upstream hardcodes them and connects with\"\necho \"     no host at all, i.e. a local socket that does not exist in a container.\"\necho \"-----------------------------------------\"\n",
            "container": "ghcr.io/parkervcp/installers:debian",
            "entrypoint": "/bin/bash"
        }
    },
    "variables": [
        {
            "name": "[REQUIRED] Steam Username",
            "description": "A real Steam account that OWNS Arma 2 and Arma 2: Operation Arrowhead. Neither appid (33900, 33930) is available to the anonymous account.",
            "env_variable": "STEAM_USER",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "required|string",
            "field_type": "text"
        },
        {
            "name": "[REQUIRED] Steam Password",
            "description": "Password for the Steam account above. Stored in the panel database in plain text -- as with every Pterodactyl egg, since the panel has no credential store.",
            "env_variable": "STEAM_PASS",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "required|string",
            "field_type": "text"
        },
        {
            "name": "Server Name",
            "description": "Server name shown in the in-game server browser. Written into the server config on every boot.",
            "env_variable": "SERVER_NAME",
            "default_value": "DayZ Mod Server",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Max Players",
            "description": "Maximum simultaneous players. DayZ Mod servers historically ran 50-60.",
            "env_variable": "MAX_PLAYERS",
            "default_value": "50",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|integer|gt:0",
            "field_type": "text"
        },
        {
            "name": "Server Password",
            "description": "Join password. Leave empty for a public server.",
            "env_variable": "SERVER_PASSWORD",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Admin Password",
            "description": "In-game admin password. Also used as the BattlEye RCON password when DISABLE_BATTLEYE=0.",
            "env_variable": "ADMIN_PASSWORD",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "World",
            "description": "Startup world, passed as -world=. 'empty' lets the loaded mission decide, which is what a plain Arma 2 server usually wants; a mod-driven server sets it explicitly (DayZ Mod uses Chernarus). A non-default map needs its matching addon installed. Chernarus is correct for stock DayZ Mod.",
            "env_variable": "WORLD",
            "default_value": "Chernarus",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string",
            "field_type": "text"
        },
        {
            "name": "Client Mods",
            "description": "Semicolon-separated mod folders loaded for everyone, e.g. @CBA;@ACE. Passed as -mod=. Entries whose folders are missing are dropped at boot rather than killing startup.",
            "env_variable": "MODIFICATIONS",
            "default_value": "@hive;@dayz",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string",
            "field_type": "text"
        },
        {
            "name": "[Advanced] Server-Side Mods",
            "description": "Semicolon-separated server-only mod folders. Passed as -serverMod=.",
            "env_variable": "SERVERMODS",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Beta Patch Path",
            "description": "The OA beta patch chain, passed as -beta=. DayZ Mod requires the beta executable path; without it the mod fails to load. Rarely needs changing.",
            "env_variable": "BETA_PATH",
            "default_value": "expansion/beta;expansion/beta/expansion",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string",
            "field_type": "text"
        },
        {
            "name": "Profile Directory",
            "description": "Profile and config directory, used for both -profiles and -name. The config file paths above are relative to it.",
            "env_variable": "PROFILE_DIR",
            "default_value": "cfgdayz",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string",
            "field_type": "text"
        },
        {
            "name": "Extra Startup Parameters",
            "description": "Appended to the launch command verbatim. -cpuCount is the usual one to tune, and -showScriptErrors helps while setting a server up. Note the 32-bit engine cannot address more than ~2 GB whatever -maxMem says.",
            "env_variable": "STARTUP_PARAMS",
            "default_value": "-noSound -noPause -cpuCount=2 -showScriptErrors",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "[Advanced] Server Binary",
            "description": "The Linux engine binary from the A2OA server package. NOT arma2oaserver, which is a start/stop/watchdog init script and cannot run in the foreground.",
            "env_variable": "SERVER_BINARY",
            "default_value": "server",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string",
            "field_type": "text"
        },
        {
            "name": "Enable Hive Persistence",
            "description": "Pipe server output through ./writer.pl so player and vehicle state is written to MySQL. Set 0 to run a throwaway non-persistent server.",
            "env_variable": "HIVE_ENABLED",
            "default_value": "1",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|boolean",
            "field_type": "text"
        },
        {
            "name": "Steam Guard Code or 2FA URL",
            "description": "Steam Guard 2FA, as EITHER a literal 5-character code OR a URL that returns one -- the mode is inferred from the value, so there is one setting and no precedence rule to remember. A URL is strongly preferred: a literal code is single-use and expires in about 30 seconds, while a URL is re-minted before every login and again after the 31-second retry wait. Any ArchiSteamFarm endpoint works, e.g. https://asf.host/api/bot/<bot>/twoFactorAuthentication/token?password=<ipc>, and both the header and query-string forms of the IPC secret are sent. The file .steam_auth in the server root is honoured too and outlives this variable -- prefer it when the URL embeds a secret, since panel variables are readable by any panel admin. A literal code there is consumed after use; a URL is kept.",
            "env_variable": "STEAM_AUTH",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Verify Signatures",
            "description": "2 requires every addon to be signed and its .bikey present in keys/ (boot.sh syncs keys out of mod folders automatically). 0 disables checking -- testing only.",
            "env_variable": "VERIFY_SIGNATURES",
            "default_value": "2",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|string|in:0,1,2",
            "field_type": "text"
        },
        {
            "name": "Sync Mod Signature Keys",
            "description": "On each boot, copy *.bikey from every @mod folder into keys/. Leave on unless you manage keys/ by hand -- with VERIFY_SIGNATURES=2 and no keys, every client loading that mod is rejected.",
            "env_variable": "SYNC_MOD_KEYS",
            "default_value": "1",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|boolean",
            "field_type": "text"
        },
        {
            "name": "Required Build",
            "description": "Client build number required to join. DayZ Mod 1.8.0.3 expects the 108074 beta patch; 0 disables the check. A wrong value here rejects every client.",
            "env_variable": "REQUIRED_BUILD",
            "default_value": "108074",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|numeric",
            "field_type": "text"
        },
        {
            "name": "Disable BattlEye",
            "description": "The SOLE decider for BattlEye. 1 = BattlEye = 0 in server.cfg and beserver.cfg removed: no BattlEye, no RCON. 0 = BattlEye = 1 and beserver.cfg written from Admin Password and RCON Port. Anything other than an explicit 0 is treated as 1. Nothing is binary-patched here, unlike the dayz-standalone egg: a 2010 engine has no BattlEye global-ban network to hide from, and the switch exists only because BattlEye is an extra moving part that often breaks modded Arma 2 servers.",
            "env_variable": "DISABLE_BATTLEYE",
            "default_value": "1",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|boolean",
            "field_type": "text"
        },
        {
            "name": "BattlEye RCON Port",
            "description": "Port BattlEye listens on for RCON when DISABLE_BATTLEYE=0. Needs its own UDP allocation in the panel. Ignored entirely when BattlEye is disabled, since nothing binds it.",
            "env_variable": "RCON_PORT",
            "default_value": "2306",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|integer",
            "field_type": "text"
        },
        {
            "name": "Hive DB Host",
            "description": "MySQL host for the Hive. Must be reachable FROM the container -- 127.0.0.1 will not work, since nothing but the game server runs in it. boot.sh patches writer.pl to use this: upstream hardcodes its credentials as Perl constants and connects with no host at all, i.e. a local socket, so unpatched it can never reach any database.",
            "env_variable": "HIVE_DB_HOST",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Hive DB Port",
            "description": "MySQL port.",
            "env_variable": "HIVE_DB_PORT",
            "default_value": "3306",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|integer",
            "field_type": "text"
        },
        {
            "name": "Hive DB Name",
            "description": "Database holding the schema from database.sql.",
            "env_variable": "HIVE_DB_NAME",
            "default_value": "dayz",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Hive DB User",
            "description": "MySQL username.",
            "env_variable": "HIVE_DB_USER",
            "default_value": "dayz",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "Hive DB Password",
            "description": "MySQL password.",
            "env_variable": "HIVE_DB_PASS",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|string",
            "field_type": "text"
        },
        {
            "name": "DayZ Mod Download URL",
            "description": "Archive containing @dayz. Takes precedence over the Steam download, and is how you run a specific community build (1.8.0.3 pairs with the @hive here).",
            "env_variable": "DAYZ_MOD_URL",
            "default_value": "",
            "user_viewable": true,
            "user_editable": true,
            "rules": "nullable|url",
            "field_type": "text"
        },
        {
            "name": "Get DayZ Mod from Steam",
            "description": "Fall back to appid 224580 when no URL is set. Free to own, but still needs the real account, and the Steam build is newer than the @hive this egg installs.",
            "env_variable": "DAYZ_MOD_FROM_STEAM",
            "default_value": "1",
            "user_viewable": true,
            "user_editable": true,
            "rules": "required|boolean",
            "field_type": "text"
        },
        {
            "name": "[Advanced] Linux Server Package URL",
            "description": "The Arma 2 OA Linux server package -- Bohemia's 1.59.84216 BETA build, mirrored because it is no longer distributed. Steam only ships a Windows dedicated server (appid 33935).",
            "env_variable": "A2OA_SERVER_URL",
            "default_value": "https://github.com/Bluscream/arma2-oa-linux-server/releases/latest/download/a2oa-server.tar.bz2",
            "user_viewable": false,
            "user_editable": false,
            "rules": "required|url",
            "field_type": "text"
        },
        {
            "name": "[Advanced] Hive / Linux Layout URL",
            "description": "Tarball supplying @hive, writer.pl, tolower.c, database.sql and the stock mission. Defaults to denisio/Dayz-Linux-Server (DayZ Mod 1.8.0.3).",
            "env_variable": "HIVE_REPO_URL",
            "default_value": "https://github.com/denisio/Dayz-Linux-Server/archive/refs/heads/master.tar.gz",
            "user_viewable": false,
            "user_editable": false,
            "rules": "required|url",
            "field_type": "text"
        },
        {
            "name": "[Advanced] Extra Flags for SteamCMD",
            "description": "Extra flags appended to every steamcmd app_update call.",
            "env_variable": "STEAMCMD_EXTRA_FLAGS",
            "default_value": "",
            "user_viewable": false,
            "user_editable": false,
            "rules": "nullable|string",
            "field_type": "text"
        }
    ]
}